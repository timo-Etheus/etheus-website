import type { Metadata } from "next";
import localFont from "next/font/local";
import "./globals.css";

const geistSans = localFont({
  src: "./fonts/GeistVF.woff",
  variable: "--font-geist-sans",
  weight: "100 900",
});
const geistMono = localFont({
  src: "./fonts/GeistMonoVF.woff",
  variable: "--font-geist-mono",
  weight: "100 900",
});

export const metadata: Metadata = {
  title: "Etheus Systems | Innovative Digital Solutions",
  description: "Etheus Systems delivers cutting-edge software development, cloud solutions, and digital transformation services that drive business growth.",
  keywords: ["software development", "cloud solutions", "digital transformation", "web development", "mobile apps"],
  openGraph: {
    title: "Etheus Systems | Innovative Digital Solutions",
    description: "Transforming ideas into digital reality with cutting-edge software solutions.",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className="scroll-smooth">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased font-sans`}
      >
        {children}
      </body>
    </html>
  );
}
